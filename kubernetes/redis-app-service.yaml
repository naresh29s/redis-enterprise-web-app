apiVersion: v1
kind: Service
metadata:
  name: redis-app-loadbalancer
  namespace: app-naresh
  labels:
    app: redis-app
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: python-redis
